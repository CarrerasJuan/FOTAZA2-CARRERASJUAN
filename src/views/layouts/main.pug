doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title= title ? `${title} | Fotaza` : "Fotaza"
    link(rel="stylesheet" href="/css/main.css")
  body(class=pageClass || "page-default")
    include ../includes/navbar
    main.app-shell
      block content
